# V3: Cross-task pseudo-label verification.
# Reject a det box if the seg head assigns < ctpv_thresh fraction of its
# pixels to the matching seg class.
_BASE_: "./ctcmt_mtl_panoptic_fpn_R_50_ACDC.yaml"

SOLVER:
  CTCMT_CTPV_ENABLED: True
  CTCMT_CTPV_THRESH: 0.25

OUTPUT_DIR: "/workspace/output/ctcmt_v3_ctpv_pfn_R50_C2ACDC"
