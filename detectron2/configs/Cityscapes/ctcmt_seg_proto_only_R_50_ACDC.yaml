# Table 2 ablation: proto only, no V2 (backbone_rst_factor=1.0), no aug.
# Shows proto contribution independently of V2.
_BASE_: "./ctcmt_seg_semfpn_R_50_ACDC.yaml"

SOLVER:
  CTCMT_BACKBONE_RST_FACTOR: 1.0
  CTCMT_SEG_PROTO_CONF_THRESH: 0.95

OUTPUT_DIR: "/workspace/output/ctcmt_seg_proto_only_R50_C2ACDC"
